[project]
name = "simulator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.4.4",
    "pre-commit>=4.6.0",
    "ruff>=0.15.12",
]

[dependency-groups]
dev = [
    "scipy>=1.17.1",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
extend-select = ["E501"]
